PORT=4000

# Bate com as credenciais default do docker-compose.yml
DATABASE_URL=postgresql://aurencare:aurencare@localhost:5432/aurencare

# Token compartilhado entre Next.js (server-side) e Express
INTERNAL_API_TOKEN=change-me-to-a-long-random-string

# Origem permitida pelo CORS (a app Next.js)
WEB_ORIGIN=http://localhost:3000
