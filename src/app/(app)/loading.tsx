import { LoadingOverlay } from '@/components/brand/Feedback'

/** Mostrado durante transições de rota / carregamento de dados no app. */
export default function Loading() {
  return <LoadingOverlay />
}
